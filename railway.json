{
  "build": {
    "command": "apt-get update && apt-get install -y libpq-dev && pip install -r requirements.txt"
  },
  "start": {
    "commands": [
      "gunicorn Clothes_app.wsgi --bind 0.0.0.0:$PORT",
      "celery -A Clothes_app worker -l info -P eventletv"
    ]
  }
}